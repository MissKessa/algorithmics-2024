package algstudent.s4;

import java.util.HashMap;

public class Graph { // COMPLEXITY: O( n ) where n is the total amount of countries
	public static HashMap<String, Node> conversion = new HashMap<String, Node>();
	HashMap<String, String> solution;  //For this we must check that(example):
										  // Spain(red): portugal(blue), france(blue)
										  // Portugal(blue): spain(red)
										  // France( blue ): spain(red)
	
	
	
}
